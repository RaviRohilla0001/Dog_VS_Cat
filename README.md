# Dog_VS_Cat
Built a CNN model using TensorFlow/Keras to classify cat and dog images. Applied image preprocessing, data augmentation, convolutional layers, pooling, dropout, and binary classification. Trained the model and evaluated performance using accuracy and loss curves.
